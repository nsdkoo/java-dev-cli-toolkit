spring:
  application:
    name: dev-cli-toolkit
  main:
    banner-mode: off

logging:
  level:
    root: INFO
    com.app.devcli: DEBUG

app:
  cli:
    name: dev-cli-toolkit
    version: 0.0.1-SNAPSHOT
